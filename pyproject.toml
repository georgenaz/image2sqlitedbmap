[project]
name = "image2sqlitedbmap"
version = "0.1.0"
description = "Pack image to sqlitedb"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ipython>=9.8.0",
    "pillow>=12.0.0",
]
